{
  "name": "cursus",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cursus — Plateforme de gestion et de suivi pour les instituts et centres de formation en Algérie",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "lint": "echo 'Add ESLint here if desired'",
    "migrate": "node scripts/migrate.mjs",
    "seed": "node scripts/migrate.mjs --seed",
    "dev:api": "node scripts/dev-api.mjs",
    "dev:all": "node scripts/dev-api.mjs & vite"
  },
  "dependencies": {
    "chart.js": "^4.4.4",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
